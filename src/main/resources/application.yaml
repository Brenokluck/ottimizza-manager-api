spring:
  profiles:
    active: dev
  application:
    name: api_manager
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/api-manager
    username: postgres
    password: password
server:
  servlet:
    context-path: /api